# Developer packaging only. End users should use files in dist/ (see README).
# This script must never copy from or write into ~/Desktop/Chris-Custom-FLC.

echo "Use npm run dist:linux (or dist:win / dist:mac). See README.md." >&2
exit 1
